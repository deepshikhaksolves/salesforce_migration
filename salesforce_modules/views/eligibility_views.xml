<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<data>
        <!-- model_eligibility form view -->
        <record id="eligibility_layout_form" model="ir.ui.view">
            <field name="name">Eligibility Layout</field>
            <field name="model">model_eligibility</field>
            <field name="arch" type="xml">
                <form string="Eligibility Form">
                    <sheet>
                        <group>
                            <group>
                                <field name="name" />
                                <field name="create_uid"/>
                            </group>
                            <group>
                                <field name="write_uid"/>
                            </group>
                        </group>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers"/>
                        <field name="activity_ids" widget="mail_activity"/>
                        <field name="message_ids" widget="mail_thread"/>
                    </div>
                </form>
            </field>
        </record>

        <!-- model_eligibility tree view -->
        <record id="eligibility_layout_tree" model="ir.ui.view">
            <field name="name">Eligibility Layout Tree</field>
            <field name="model">model_eligibility</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name" />
                    <field name="create_uid"/>
                </tree>
            </field>
        </record>

        <!-- model_eligibility action window -->
        <record id="eligibility_action" model="ir.actions.act_window">
            <field name="name">Eligibility Layout</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">model_eligibility</field>
            <field name="view_mode">tree,form</field>
            <field name="view_ids" eval="[(5, 0, 0), 
                (0, 0, {'view_mode': 'tree', 'view_id': ref('eligibility_layout_tree')}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('eligibility_layout_form')})]"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No records available.
                </p>
            </field>
        </record>
    </data>
</odoo>